@page "/logout"
@inject AuthenticationStateProvider provider

@code {
    protected override async Task OnInitializedAsync()
    {
        await ((JwtAuthenticationStateProvider)provider).MakeUserAnonimousAsync();
    }
}
